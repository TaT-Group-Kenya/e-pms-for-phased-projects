import DocsLayout from '../../components/docs/DocsLayout';

export default function QuotationsDoc() {
  return (
    <DocsLayout>
      <h1 className="text-2xl font-bold mb-4">Quotations</h1>
      <ol className="list-decimal pl-6 space-y-4">
        <li>
          <strong>View Quotations</strong>
          <div className="mt-2">Go to the Quotations section.</div>
          <img src="https://placehold.co/600x300?text=Quotations+List" alt="Quotations List" className="my-4 rounded shadow" />
        </li>
        <li>
          <strong>Create a Quotation</strong>
          <div className="mt-2">Click "Create Quotation" and fill in the form.</div>
          <img src="https://placehold.co/600x300?text=Create+Quotation" alt="Create Quotation" className="my-4 rounded shadow" />
        </li>
      </ol>
    </DocsLayout>
  );
}
